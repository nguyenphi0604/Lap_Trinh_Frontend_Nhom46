services:
  frontend:
    build: .
    ports:
      - "3000:80"
    depends_on:
      - api

  api:
    image: node:20-alpine
    working_dir: /data
    volumes:
      - .:/data
    ports:
      - "5000:5000"
    command: npx json-server --watch db.json --port 5000